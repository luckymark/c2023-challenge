#include "is_prime.h"

int main() {
    func();

    /*
    for(int i = 1; i <= 100; i ++){
        if(isPrimeNumber(i)){

            if((i - 2) > 0){
                printf(", ");
            }

            printf("%d, ", i);
        }
    }
    */
    // printf("\b\b  ");

    return 0;
}

